//Declare Variables

//Wins starts at zero and increments with every win over the computer

//Losses start at zero and increments with every loss to the the computer

//Total Guesses is 10, so this will decrement with every guess

//Guesses so far will store the user input and display each key press until a win or loss

//After a win or loss the game will reset without a page refresh